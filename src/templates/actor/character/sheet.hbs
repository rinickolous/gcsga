<form class="{{cssClass}} gcsga" autocomplete="off">
	<div id="sheet">
		<div id="personal">
			{{>portrait}}
			{{>identity}}
			{{>miscellaneous}}
			{{>description}}
			{{>points}}
		</div>
		<div id="stats">
			<div id="attributes">
				{{>primary-attributes}}
				{{>basic-damage}}
				{{>secondary-attributes}}
				{{>pool-attributes}}
			</div>
			<div id="hit-locations">
				<div class="header body-plan">{{data.settings.body_type.name}}</div>
				<div class="header roll">{{localize "gcsga.character.body_type.roll"}}</div>
				<div class="header location">{{localize "gcsga.character.body_type.location"}}</div>
				<div class="header dr">{{localize "gcsga.character.body_type.dr"}}</div>
				{{#each data.settings.body_type.locations}}
				{{>hit-location tab=""}}
				{{/each}}
			</div>
			<div id="encumbrance-lifting">
				{{>encumbrance}}
				{{>lifting}}
			</div>
		</div>
		<div id="embeds" style="grid-template: {{blockLayout settings.block_layout blocks}};">
			{{#if reactions.length}}
			<div id="reactions">
				<div class="header modifier">{{localize "gcsga.character.reactions.modifier"}}</div>
				<div class="header condition">{{localize "gcsga.character.reactions.reaction"}}</div>
				{{#each reactions}}
				{{>reaction}}
				{{/each}}
			</div>
			{{/if}}
			{{#if conditionalModifiers.length}}
			<div id="conditional-modifiers">
				<div class="header modifier">{{localize "gcsga.character.conditional_modifiers.modifier"}}</div>
				<div class="header condition">{{localize "gcsga.character.conditional_modifiers.reaction"}}</div>
				{{#each conditionalModifiers}}
				{{>conditional-modifier}}
				{{/each}}
			</div>
			{{/if}}
			{{#if melee.length}}
			<div id="melee">
				<div class="header desc">{{localize "gcsga.character.melee.desc"}}</div>
				<div class="header usage">{{localize "gcsga.character.melee.usage"}}</div>
				<div class="header level">{{localize "gcsga.character.melee.level"}}</div>
				<div class="header parry">{{localize "gcsga.character.melee.parry"}}</div>
				<div class="header block">{{localize "gcsga.character.melee.block"}}</div>
				<div class="header damage">{{localize "gcsga.character.melee.damage"}}</div>
				<div class="header reach">{{localize "gcsga.character.melee.reach"}}</div>
				<div class="header st">{{localize "gcsga.character.melee.st"}}</div>
				{{#each melee}}
				{{>melee-attack}}
				{{/each}}
			</div>
			{{/if}}
			{{#if ranged.length}}
			<div id="ranged">
				<div class="header desc">{{localize "gcsga.character.ranged.desc"}}</div>
				<div class="header usage">{{localize "gcsga.character.ranged.usage"}}</div>
				<div class="header level">{{localize "gcsga.character.ranged.level"}}</div>
				<div class="header accuracy">{{localize "gcsga.character.ranged.accuracy"}}</div>
				<div class="header damage">{{localize "gcsga.character.ranged.damage"}}</div>
				<div class="header range">{{localize "gcsga.character.ranged.range"}}</div>
				<div class="header rate-of-fire">{{localize "gcsga.character.ranged.rate_of_fire"}}</div>
				<div class="header shots">{{localize "gcsga.character.ranged.shots"}}</div>
				<div class="header bulk">{{localize "gcsga.character.ranged.bulk"}}</div>
				<div class="header recoil">{{localize "gcsga.character.ranged.recoil"}}</div>
				<div class="header st">{{localize "gcsga.character.ranged.st"}}</div>
				{{#each ranged}}
				{{>ranged-attack}}
				{{/each}}
			</div>
			{{/if}}
			{{#if traits.length}}
			<div id="traits" class="item-list">
				<div class="header desc">{{localize "gcsga.character.traits.desc"}}</div>
				<div class="header points">{{localize "gcsga.character.traits.points"}}</div>
				<div class="header reference"><i class="gcs-bookmark"></i></div>
				{{#each traits}}
				{{>trait}}
				{{/each}}
			</div>
			{{/if}}
			{{#if skills.length}}
			<div id="skills" class="item-list">
				<div class="header desc">{{localize "gcsga.character.skills.desc"}}</div>
				<div class="header level">{{localize "gcsga.character.skills.level"}}</div>
				<div class="header rsl">{{localize "gcsga.character.skills.rsl"}}</div>
				<div class="header points">{{localize "gcsga.character.skills.points"}}</div>
				<div class="header reference"><i class="gcs-bookmark"></i></div>
				{{#each skills}}
				{{>skill}}
				{{/each}}
			</div>
			{{/if}}
			{{#if spells.length}}
			<div id="spells" class="item-list">
				<div class="header desc">{{localize "gcsga.character.spells.desc"}}</div>
				<div class="header college">{{localize "gcsga.character.spells.college"}}</div>
				<div class="header level">{{localize "gcsga.character.spells.level"}}</div>
				<div class="header rsl">{{localize "gcsga.character.spells.rsl"}}</div>
				<div class="header points">{{localize "gcsga.character.spells.points"}}</div>
				<div class="header reference"><i class="gcs-bookmark"></i></div>
				{{#each spells}}
				{{>spell}}
				{{/each}}
			</div>
			{{/if}}
			{{#if equipment.length}}
			<div id="equipment" class="item-list">
				<div class="header equipped"><i class="gcs-checkmark"></i></div>
				<div class="header quantity">{{localize "gcsga.character.equipment.quantity"}}</div>
				<div class="header desc">{{localize "gcsga.character.equipment.desc" weight=actor.fastWeightCarried
					value=actor.fastWealthCarried}}</div>
				<div class="header uses">{{localize "gcsga.character.equipment.uses"}}</div>
				<div class="header tech-level">{{localize "gcsga.character.equipment.tech_level"}}</div>
				<div class="header legality-class">{{localize "gcsga.character.equipment.legality_class"}}</div>
				<div class="header value"><i class="gcs-coins"></i></div>
				<div class="header weight"><i class="gcs-weight"></i></div>
				<div class="header extended-value"><i class="gcs-stack"></i><i class="gcs-coins"></i></div>
				<div class="header extended-weight"><i class="gcs-stack"></i><i class="gcs-weight"></i></div>
				<div class="header reference"><i class="gcs-bookmark"></i></div>
				{{#each equipment}}
				{{>equipment}}
				{{/each}}
			</div>
			{{/if}}
			{{#if other_equipment.length}}
			<div id="other-equipment" class="item-list">
				<div class="header quantity">{{localize "gcsga.character.other_equipment.quantity"}}</div>
				<div class="header desc">{{localize "gcsga.character.other_equipment.desc" value=other_value}}</div>
				<div class="header uses">{{localize "gcsga.character.other_equipment.uses"}}</div>
				<div class="header tech-level">{{localize "gcsga.character.other_equipment.tech_level"}}</div>
				<div class="header legality-class">{{localize "gcsga.character.other_equipment.legality_class"}}</div>
				<div class="header value"><i class="gcs-coins"></i></div>
				<div class="header weight"><i class="gcs-weight"></i></div>
				<div class="header extended-value"><i class="gcs-stack"></i><i class="gcs-coins"></i></div>
				<div class="header extended-weight"><i class="gcs-stack"></i><i class="gcs-weight"></i></div>
				<div class="header reference"><i class="gcs-bookmark"></i></div>
				{{#each other_equipment}}
				{{>other-equipment}}
				{{/each}}
			</div>
			{{/if}}
			{{#if notes.length}}
			<div id="notes" class="item-list">
				<div class="header desc">{{localize "gcsga.character.notes.desc"}}</div>
				<div class="header reference"><i class="gcs-bookmark"></i></div>
				{{#each notes}}
				{{>note}}
				{{/each}}
			</div>
			{{/if}}
		</div>
		<div id="footer">
			<div class="left">{{{localize "gcsga.character.copyright" current_year=current_year}}}</div>
			<div class="right"><a href="https://gurpscharactersheet.com/">gurpscharactersheet.com</a></div>
		</div>
	</div>
</form>
