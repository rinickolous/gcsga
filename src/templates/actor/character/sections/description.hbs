<div id="description">
	<div class="header">{{localize "gcsga.character.description"}}</div>
	<div class="fieldblock">
		{{>input-text label="gcsga.character.gender" field="field" name="system.profile.gender" type="String"
		data=data.profile.gender class=(input_lock editing)}}
		{{>input-text label="gcsga.character.age" field="field" name="system.profile.age" type="String"
		data=data.profile.age class=(input_lock editing)}}
		{{>input-text label="gcsga.character.birthday" field="field" name="system.profile.birthday" type="String"
		data=data.profile.birthday class=(input_lock editing)}}
		{{>input-text label="gcsga.character.religion" field="field" name="system.profile.religion" type="String"
		data=data.profile.religion class=(input_lock editing)}}
	</div>
	<div class="fieldblock">
		{{>input-text label="gcsga.character.height" field="field" name="system.profile.height" type="String"
		data=data.profile.height class=(input_lock editing)}}
		{{>input-text label="gcsga.character.weight" field="field" name="system.profile.weight" type="String"
		data=data.profile.weight class=(input_lock editing)}}
		{{>input-text label="gcsga.character.sm" field="field" name="system.profile.SM" type="Number" data=(signed
		actor.adjustedSizeModifier) class=(input_lock editing)}}
		{{>input-text label="gcsga.character.tech_level" field="field" name="system.profile.tech_level" type="String"
		data=data.profile.tech_level class=(input_lock editing)}}
	</div>
	<div class="fieldblock">
		{{>input-text label="gcsga.character.hair" field="field" name="system.profile.hair" type="String"
		data=data.profile.hair class=(input_lock editing)}}
		{{>input-text label="gcsga.character.eyes" field="field" name="system.profile.eyes" type="String"
		data=data.profile.eyes class=(input_lock editing)}}
		{{>input-text label="gcsga.character.skin" field="field" name="system.profile.skin" type="String"
		data=data.profile.skin class=(input_lock editing)}}
		{{>input-text label="gcsga.character.handedness" field="field" name="system.profile.handedness" type="String"
		data=data.profile.handedness class=(input_lock editing)}}
	</div>
</div>
