{{#if label}}
<div class="label align-right">{{localize label}}</div>
{{/if}}
{{#if field}}
<div class="{{field}}">
{{/if}}
<input
	name="{{name}}"
	class="input"
	type="text"
	data-dtype="{{type}}"
	value="{{data}}"
	{{{class}}}
/>
{{#if field}}
</div>
{{/if}}