<div class="desc item" data-entry-uuid="Compendium.{{this.compendium}}.{{this._id}}" data-type="Item" {{{indent
	this.parents.length}}}>
	<div class="dropdown">
		{{#if (eq this.type "trait_container")}}
		{{#if this.open}}
		{{>actor/character/sections/dropdown-open}}
		{{else}}
		{{>actor/character/sections/dropdown-closed}}
		{{/if}}
		{{/if}}
	</div>
	{{!-- {{this.name_formatted}} --}}
	<div class="name">
		{{this.name}}
	</div>
	{{>actor/character/sections/item-notes}}
</div>
<div class="points">{{this.adjustedPoints}}</div>
<div class="tags">{{join this.tags ", "}}</div>
<div class="reference" data-pdf="{{this.reference}}">{{ref this.reference}}</div>
{{#if this.open}}
{{#each (sort this.children "sort")}}
{{>compendium-browser/trait}}
{{/each}}
{{/if}}
