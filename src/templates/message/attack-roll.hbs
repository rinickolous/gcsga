<div class="message-roll attack">
	<div class="name">{{name}} ({{level}})</div>
	<div class="total">{{#each rolls}}
		{{#unless (eq @index 0)}} + {{/unless}}
		<i data-result={{this.result}} class="rollable fas fa-dice-{{this.word}}"></i>
		{{/each}}
		{{#if modifier}}{{#if (gt modifier 0)}} + {{modifier}}{{else}} - {{abs modifier}}{{/if}}{{/if}}
		= {{total}}
	</div>
	<div class="result {{success}}">{{localize (concat "gurps.roll.success_attack." success)}}</div>
	<div class="margin">
		{{#if (in success "success")}}
		{{#if (eq margin 0)}}
		{{localize "gurps.roll.just_made_it"}}
		{{else}}
		{{localize "gurps.roll.success_margin" margin=margin}}
		{{/if}}
		{{else}}
		{{localize "gurps.roll.failure_margin" margin=margin}}
		{{/if}}
	</div>
	{{!-- <div data-actor-id="{{actor.id}}" data-item-id="{{item.id}}" data-attack-id="{{weapon.id}}" data-type="damage"
		--}} {{!-- class="damage rollable">Roll --}}
		{{!-- Damage</div> --}}
</div>
